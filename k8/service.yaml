apiVersion: v1
kind: Service
metadata:
  name: employee
spec:
  selector:
    app: employee
  type: ClusterIP
  ports:
    - port: 80          # 👈 STANDARD SERVICE PORT
      targetPort: 8080
      protocol: TCP
