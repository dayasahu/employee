apiVersion: v1
kind: Service
metadata:
  name: employee
spec:
  selector:
    app: employee
    app.kubernetes.io/part-of: microservices-training
    environment: dev
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
