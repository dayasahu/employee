<?xml version="1.0" encoding="UTF-8"?>
<!--
    Two appenders:
      STDOUT - JSON via LogstashEncoder (existing behaviour preserved)
      OTEL   - OpenTelemetry OTLP appender that ships logs to the collector

    The OTLP appender forwards records via the global OpenTelemetry SDK, so
    its endpoint comes from Spring Boot's management.otlp.* properties.
-->
<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>spanId</includeMdcKeyName>
        </encoder>
    </appender>

    <appender name="OTEL"
              class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
        <captureExperimentalAttributes>true</captureExperimentalAttributes>
        <captureMdcAttributes>*</captureMdcAttributes>
        <captureCodeAttributes>true</captureCodeAttributes>
    </appender>

    <root level="info">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="OTEL"/>
    </root>
</configuration>
